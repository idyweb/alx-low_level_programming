#include <stdio.h>
/**
 * main - a program that prints numbers in a range of 10.
 * Return: (0) as success
 *
 */
int main(void)

{
	int x;

	for (x = 0; x < 10; x++)

		printf("%d\n", x);

	return (0);
}
