#include "main.h"
/**
 * add - function that adds two integer and return result
 * @x: the first number
 * @y: the second number
 * Return: is the result
 */
int add(int x, int y)

{
	return (x + y);
}
